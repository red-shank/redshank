---
title: 'Input'
description: 'A basic widget for getting the user input is a text field. Keyboard and mouse can be used for providing or changing data.'
url: https://redshank.app/docs/components/input
---

```jsx
import { Input } from '@redshank/native';
```

### When To Use
* A user input in a form field is needed.
* A search input is required.

<Spacer y={2} />

### Examples

<Playground
  id="default"
  title="Default"
  description="Default input element."
  code={input.defaultCode}
/>

<Spacer y={2} />

<Playground
  id="sizes"
  title="Sizes"
  description="Sizes input element."
  code={input.sizes}
/>

<Spacer y={2} />

<Playground
  id="prefix-and-suffix"
  title="Prefix and Suffix"
  description="Prefix and Suffix element."
  code={input.prefixSuffix}
/>

<Spacer y={2} />

<Playground
  id="errors"
  title="Errors"
  description="With errors element."
  code={input.errors}
/>

<Spacer y={2} />

<Playground
  id="number"
  title="Number"
  description="Only number element."
  code={input.number}
/>

<Spacer y={2} />

<Playground
  id="textarea"
  title="Text Area"
  description="Textarea element."
  code={input.textArea}
/>

<Spacer y={2} />

### APIs

<Spacer y={2} />

#### Input
Api props extends from [TextInput](https://reactnative.dev/docs/textinput)

| Attribute            | Type                      | Accepted values                   | Description                                         | Default                                 |
|----------------------|---------------------------|-----------------------------------|-----------------------------------------------------|-----------------------------------------|
| **background**       | `ThemeColor` `string`     | [ThemeColor](/docs/default-theme) | Change background color of the input                | `inputColor`                            |
| **borderInputColor** | `ThemeColor` `string`     | [ThemeColor](/docs/default-theme) | Change border color of the input                    | `border`                                |
| **color**            | `ThemeColor` `string`     | [ThemeColor](/docs/default-theme) | Change text color of the input                      | `accents2`                              |
| **error**            | `boolean`                 | `true` `false`                    | Show error variant                                  | -                                       |
| **isDisabled**       | `boolean`                 | `true` `false`                    | Editable or Disabled input                          | -                                       |
| **onChange**         | `function`                | `(value: any) => void`            | On change function                                  | -                                       |
| **placeholder**      | `string`                  | `Text` `string`                   | Placeholder text for the Input                      | -                                       |
| **placeholderColor** | `ThemeColor` `string`     | [ThemeColor](/docs/default-theme) | Placeholder color for the Input                     | `border`                                |
| **prefix**           | `ReactNode` `JSX.Element` | `Component`                       | Prefix icon                                         | -                                       |
| **showIcon**         | `boolean`                 | `true` `false`                    | Hidden or Show icon, util when type is `password`   | `false` and `true` when type="password" |
| **size**             | `SizeType`                | `small` `middle` `large`          | Size element customizable in `ThemeProvider`        | `middle`                                |
| **suffix**           | `ReactNode` `JSX.Element` | `Component`                       | Suffix icon                                         | -                                       |
| **textError**        | `string`                  | `Text`                            | Message of the error, set with error prop in `true` | -                                       |
| **type**             | `InputType`               | [Input Type](#input-type)         | Type of the input                                   | `default`                               |
| **withMarginBottom** | `boolean`                 | `true` `false`                    | Insert margin bottom if your not use Form           | `false`                                 |
| **wrapperStyle**     | `object`                  | `StyleProp<ViewStyle>`            | Style for the wrapper element                       | -                                       |

<Spacer y={2} />

#### Input Type
```ts
type InputType =
| 'default'
| 'number-pad'
| 'decimal-pad'
| 'numeric'
| 'email-address'
| 'phone-pad'
| 'url'
// Custom @beauty-design
| 'password'
// iOS Only
| 'ascii-capable'
| 'numbers-and-punctuation'
| 'name-phone-pad'
| 'twitter'
| 'web-search'
// Android Only
| 'visible-password'
```
