import React from 'react';
import { TabsProps } from './types';
export declare function Tabs({ items, activeKey, error, onChange, textError, defaultActiveKey, backgroundColors, renderTabItem, scrollable, variant, styles, labelProps, size, ...sxProps }: TabsProps): React.JSX.Element;
