import React from 'react';
import { ScrollView } from '../components/ScrollView';
export const InputScrollView = (props) => {
    return <ScrollView showsVerticalScrollIndicator={false} {...props}/>;
};
